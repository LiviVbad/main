<UserControl
    x:Class="AppFramework.Views.EmailActivationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="clr-namespace:AppFramework.Extensions"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="{extensions:Translate SendEmailActivationLink_Information}" />
            <TextBox Text="{Binding EmailAddress}" />

            <Button
                Margin="0,10"
                Command="{Binding SendEmailActivationCommand}"
                Content="{extensions:Translate Submit}"
                IsEnabled="{Binding IsEmailActivationEnabled}" />

            <Button Command="{Binding CancelCommand}" Content="{extensions:Translate Cancel}" />
        </StackPanel>
    </Grid>
</UserControl>
